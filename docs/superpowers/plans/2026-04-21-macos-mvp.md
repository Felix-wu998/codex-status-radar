# macOS MVP 实施计划

> **给执行型 agent 的要求：** 必须按任务顺序执行。每个任务都遵守 TDD：先写失败测试，确认失败，再写最小实现，确认通过，再提交。除代码标识符、协议字段、命令和第三方原文外，协作说明默认使用中文。

**目标：** 做出第一个可用的 macOS MVP：本地优先、菜单栏常驻、灵动岛状态灯、精准识别 Codex `waitingOnApproval`，并在灵动岛区域展示 Codex 原生审批选项。

**架构：** `packages/core` 负责协议解析、状态归约、隐私脱敏和 view model；`apps/macos` 负责 AppKit / SwiftUI 展示、菜单栏、灵动岛窗口、WebSocket 连接和用户交互。MVP 阶段只 attach 到已有的本地 Codex app-server，不主动托管启动 Codex。

**技术栈：** Swift Package Manager、Swift 6、XCTest、AppKit、SwiftUI、URLSessionWebSocketTask、本地 Codex app-server WebSocket。

---

## 文件职责

- 修改：`Package.swift`，增加 macOS executable target 和测试 target。
- 创建：`packages/core/Sources/CodexStatusRadarCore/AppServerEnvelope.swift`，解码 app-server 事件外壳。
- 创建：`packages/core/Sources/CodexStatusRadarCore/ThreadStatus.swift`，定义本地 Codex 状态。
- 创建：`packages/core/Sources/CodexStatusRadarCore/CodexEventReducer.swift`，把 app-server 事件归约成本地状态。
- 创建：`packages/core/Sources/CodexStatusRadarCore/PrivacyRedactor.swift`，做路径、项目名和遥测字段脱敏。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/CodexStatusRadarApp.swift`，macOS app 入口。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/AppDelegate.swift`，管理应用生命周期。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/MenuBarController.swift`，管理菜单栏图标。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/CodexAppServerClient.swift`，连接 app-server。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusWindowController.swift`，管理灵动岛窗口。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusView.swift`，展示状态灯。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/ApprovalPopoverView.swift`，展示审批弹窗。
- 创建：`apps/macos/Sources/CodexStatusRadarApp/SettingsStore.swift`，管理连接、隐私和遥测设置。
- 更新：`README.md`，补运行和验证说明。
- 更新：`docs/architecture/technical-architecture.md`，记录实施中出现的架构变更。

## 任务 1：core 层 app-server 事件模型

**状态：已完成，提交 `c3d5aa6 Add app-server envelope models`。**

**目标：** 让 core 层能解码最小 app-server 事件外壳，包括 `thread/status/changed` 和 `item/commandExecution/requestApproval`。

**文件：**

- 创建：`packages/core/Sources/CodexStatusRadarCore/AppServerEnvelope.swift`
- 创建：`packages/core/Tests/CodexStatusRadarCoreTests/AppServerEnvelopeTests.swift`

**测试要求：**

- 能解码 `method`。
- 能解码 `threadId`。
- 能解码 `activeFlags`。
- 能解码 `command`。
- 能解码 `availableDecisions`，并复用 `ApprovalDecision`。

**验证命令：**

```bash
swift test --disable-sandbox --filter AppServerEnvelopeTests
swift test --disable-sandbox
```

## 任务 2：core 层状态模型和事件归约

**目标：** 把 app-server 事件转成 UI 可消费的本地状态，尤其是把 `waitingOnApproval` 转成 `.waitingForApproval`。

**文件：**

- 创建：`packages/core/Sources/CodexStatusRadarCore/ThreadStatus.swift`
- 创建：`packages/core/Sources/CodexStatusRadarCore/CodexEventReducer.swift`
- 创建：`packages/core/Tests/CodexStatusRadarCoreTests/CodexEventReducerTests.swift`

- [ ] **步骤 1：写失败测试**

测试内容：

- `thread/status/changed` 包含 `activeFlags: ["waitingOnApproval"]` 时，状态变成 `.waitingForApproval`。
- `item/commandExecution/requestApproval` 到达时，状态变成 `.waitingForApproval`，并生成 `pendingApproval`。
- `pendingApproval` 默认不展示命令正文，`commandPreview` 应为 `nil`。
- `pendingApproval.actions` 应来自 `availableDecisions` 的映射。

关键测试代码：

```swift
let envelope = AppServerEnvelope(
    method: "thread/status/changed",
    params: AppServerParams(
        threadId: "thread-1",
        turnId: nil,
        itemId: nil,
        status: "running",
        activeFlags: ["waitingOnApproval"],
        command: nil,
        cwd: nil,
        availableDecisions: nil
    )
)

var state = ProjectStatus.empty(projectName: "codex-status-radar")
state = CodexEventReducer.reduce(state, envelope: envelope)

XCTAssertEqual(state.phase, .waitingForApproval)
XCTAssertEqual(state.threadId, "thread-1")
```

- [ ] **步骤 2：确认测试失败**

```bash
swift test --disable-sandbox --filter CodexEventReducerTests
```

预期：因为 `ProjectStatus`、`CodexPhase`、`CodexEventReducer` 还不存在，编译失败。

- [ ] **步骤 3：写最小实现**

实现 `ProjectStatus`：

```swift
public struct ProjectStatus: Equatable, Sendable {
    public let projectName: String
    public let threadId: String?
    public let phase: CodexPhase
    public let pendingApproval: ApprovalRequestViewModel?

    public static func empty(projectName: String) -> ProjectStatus {
        ProjectStatus(
            projectName: projectName,
            threadId: nil,
            phase: .idle,
            pendingApproval: nil
        )
    }
}

public enum CodexPhase: Equatable, Sendable {
    case disconnected
    case idle
    case working
    case waitingForInput
    case waitingForApproval
    case completed
    case failed
}
```

实现 `CodexEventReducer`：

```swift
public enum CodexEventReducer {
    public static func reduce(
        _ state: ProjectStatus,
        envelope: AppServerEnvelope
    ) -> ProjectStatus {
        switch envelope.method {
        case "thread/status/changed":
            let waiting = envelope.params.activeFlags?.contains("waitingOnApproval") == true
            return ProjectStatus(
                projectName: state.projectName,
                threadId: envelope.params.threadId ?? state.threadId,
                phase: waiting ? .waitingForApproval : .working,
                pendingApproval: state.pendingApproval
            )
        case "item/commandExecution/requestApproval":
            return ProjectStatus(
                projectName: state.projectName,
                threadId: envelope.params.threadId ?? state.threadId,
                phase: .waitingForApproval,
                pendingApproval: ApprovalRequestViewModel(
                    projectName: state.projectName,
                    reason: nil,
                    commandPreview: nil,
                    decisions: envelope.params.availableDecisions
                )
            )
        default:
            return state
        }
    }
}
```

- [ ] **步骤 4：确认测试通过**

```bash
swift test --disable-sandbox --filter CodexEventReducerTests
swift test --disable-sandbox
```

预期：新增 reducer 测试和既有测试全部通过。

- [ ] **步骤 5：提交**

```bash
git add packages/core/Sources/CodexStatusRadarCore/ThreadStatus.swift \
  packages/core/Sources/CodexStatusRadarCore/CodexEventReducer.swift \
  packages/core/Tests/CodexStatusRadarCoreTests/CodexEventReducerTests.swift
git commit -m "Add Codex event reducer"
git push
```

## 任务 3：隐私脱敏规则

**目标：** 在进入 UI 或遥测前，把路径和项目识别信息降到最小可用范围。

**文件：**

- 创建：`packages/core/Sources/CodexStatusRadarCore/PrivacyRedactor.swift`
- 创建：`packages/core/Tests/CodexStatusRadarCoreTests/PrivacyRedactorTests.swift`

- [ ] **步骤 1：写失败测试**

测试内容：

- `/Users/example/SecretClientApp` 只显示 `SecretClientApp`。
- 遥测只返回粗粒度 bucket，例如 `local-project`。
- 空路径返回 `Unknown Project` 或 `unknown`。

- [ ] **步骤 2：确认测试失败**

```bash
swift test --disable-sandbox --filter PrivacyRedactorTests
```

- [ ] **步骤 3：写最小实现**

```swift
import Foundation

public enum PrivacyRedactor {
    public static func projectName(fromPath path: String?) -> String {
        guard let path, !path.isEmpty else {
            return "Unknown Project"
        }

        return URL(fileURLWithPath: path).lastPathComponent
    }

    public static func telemetryPathBucket(fromPath path: String?) -> String {
        guard path != nil else {
            return "unknown"
        }

        return "local-project"
    }
}
```

- [ ] **步骤 4：确认测试通过并提交**

```bash
swift test --disable-sandbox --filter PrivacyRedactorTests
swift test --disable-sandbox
git add packages/core/Sources/CodexStatusRadarCore/PrivacyRedactor.swift \
  packages/core/Tests/CodexStatusRadarCoreTests/PrivacyRedactorTests.swift
git commit -m "Add privacy redaction rules"
git push
```

## 任务 4：macOS 应用壳

**目标：** 建立可构建的 macOS app target，启动后只显示菜单栏图标，不做实际业务逻辑。

**文件：**

- 修改：`Package.swift`
- 创建：`apps/macos/Sources/CodexStatusRadarApp/CodexStatusRadarApp.swift`
- 创建：`apps/macos/Sources/CodexStatusRadarApp/AppDelegate.swift`
- 创建：`apps/macos/Sources/CodexStatusRadarApp/MenuBarController.swift`

- [ ] **步骤 1：修改 package target**

增加 executable target：

```swift
.executableTarget(
    name: "CodexStatusRadarApp",
    dependencies: ["CodexStatusRadarCore"],
    path: "apps/macos/Sources/CodexStatusRadarApp"
)
```

- [ ] **步骤 2：添加 app 入口**

```swift
import SwiftUI

@main
struct CodexStatusRadarApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) private var appDelegate

    var body: some Scene {
        Settings {
            Text("Codex Status Radar")
                .frame(width: 320, height: 160)
        }
    }
}
```

- [ ] **步骤 3：添加菜单栏控制器**

要求：

- 应用使用 `.accessory` activation policy。
- 菜单栏显示系统图标。
- 菜单包含应用名和退出按钮。

- [ ] **步骤 4：构建验证**

```bash
swift build --disable-sandbox
```

预期：构建通过。

- [ ] **步骤 5：提交**

```bash
git add Package.swift apps/macos/Sources/CodexStatusRadarApp
git commit -m "Add macOS app shell"
git push
```

## 任务 5：app-server WebSocket 客户端

**目标：** 建立 attach 到本地 Codex app-server 的客户端骨架。

**文件：**

- 创建：`apps/macos/Sources/CodexStatusRadarApp/CodexAppServerClient.swift`
- 创建：`apps/macos/Tests/CodexStatusRadarAppTests/CodexAppServerClientTests.swift`

- [ ] **步骤 1：写失败测试**

测试默认 endpoint：

```swift
XCTAssertEqual(
    CodexAppServerEndpoint.default.url.absoluteString,
    "ws://127.0.0.1:8794"
)
```

- [ ] **步骤 2：确认测试失败**

```bash
swift test --disable-sandbox --filter CodexAppServerClientTests
```

- [ ] **步骤 3：实现 endpoint 和客户端骨架**

要求：

- 默认连接 `ws://127.0.0.1:8794`。
- 支持 `connect()`。
- 支持 `disconnect()`。
- 暂不实现自动重连和回传。

- [ ] **步骤 4：验证并提交**

```bash
swift test --disable-sandbox --filter CodexAppServerClientTests
swift build --disable-sandbox
git add apps/macos/Sources/CodexStatusRadarApp/CodexAppServerClient.swift \
  apps/macos/Tests/CodexStatusRadarAppTests/CodexAppServerClientTests.swift
git commit -m "Add app-server client skeleton"
git push
```

## 任务 6：灵动岛状态灯窗口

**目标：** 显示一个无边框、靠近灵动岛区域的小状态灯窗口。

**文件：**

- 创建：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusWindowController.swift`
- 创建：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusView.swift`
- 修改：`apps/macos/Sources/CodexStatusRadarApp/AppDelegate.swift`

- [ ] **步骤 1：实现 `NotchStatusView`**

要求：

- 输入 `CodexPhase`。
- `working` 显示绿色。
- `waitingForApproval` 显示橙色。
- `waitingForInput` 显示黄色。
- `disconnected` 和 `failed` 显示红色。
- `idle` 和 `completed` 显示灰色。

- [ ] **步骤 2：实现无边框窗口控制器**

要求：

- `NSWindow` 使用 `.borderless`。
- 背景透明。
- `level` 使用 `.statusBar`。
- 默认定位到主屏顶部中央附近。

- [ ] **步骤 3：从 `AppDelegate` 启动窗口**

启动 app 后应同时出现菜单栏图标和状态灯。

- [ ] **步骤 4：构建和手动验证**

```bash
swift build --disable-sandbox
.build/debug/CodexStatusRadarApp
```

预期：菜单栏图标出现，灵动岛附近出现小状态灯。

- [ ] **步骤 5：提交**

```bash
git add apps/macos/Sources/CodexStatusRadarApp
git commit -m "Add notch status light"
git push
```

## 任务 7：灵动岛审批弹窗

**目标：** 当存在 `ApprovalRequestViewModel` 时，在灵动岛区域展示审批弹窗，按钮来自 core 映射结果。

**文件：**

- 创建：`apps/macos/Sources/CodexStatusRadarApp/ApprovalPopoverView.swift`
- 修改：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusWindowController.swift`

- [ ] **步骤 1：实现 `ApprovalPopoverView`**

要求：

- 展示项目名。
- 可展示 `reason`。
- 不默认展示 `commandPreview`。
- 遍历 `viewModel.actions` 生成按钮。
- 点击按钮时回调 `ApprovalAction`。

- [ ] **步骤 2：在窗口控制器中增加 `showApproval`**

要求：

- 把窗口内容切换成 `ApprovalPopoverView`。
- 宽度先固定为 360。
- 不在这个任务里发送真实 approval response。

- [ ] **步骤 3：构建验证**

```bash
swift build --disable-sandbox
```

- [ ] **步骤 4：手动 fixture 验证**

在本地临时注入一个 `ApprovalRequestViewModel`，确认弹窗能展示三个按钮。验证后移除 fixture，不把 fixture 提交。

- [ ] **步骤 5：提交**

```bash
git add apps/macos/Sources/CodexStatusRadarApp/ApprovalPopoverView.swift \
  apps/macos/Sources/CodexStatusRadarApp/NotchStatusWindowController.swift
git commit -m "Add approval popover UI"
git push
```

## 任务 8：接入真实审批流

**目标：** 从 app-server 接收真实 approval request，在灵动岛弹窗点击后，把原始 decision 回传给 Codex。

**文件：**

- 修改：`apps/macos/Sources/CodexStatusRadarApp/CodexAppServerClient.swift`
- 修改：`apps/macos/Sources/CodexStatusRadarApp/AppDelegate.swift`
- 修改：`apps/macos/Sources/CodexStatusRadarApp/NotchStatusWindowController.swift`
- 更新：`README.md`
- 更新：`docs/research/app-server-approval-spike.md`

- [ ] **步骤 1：客户端增加 envelope 回调**

要求：

- `CodexAppServerClient` 收到 JSON text message 后解码为 `AppServerEnvelope`。
- 解码成功后调用 `onEnvelope`。
- 解码失败先静默忽略，不上传 payload。

- [ ] **步骤 2：AppDelegate 归约状态**

要求：

- 保存当前 `ProjectStatus`。
- 每个 envelope 进入 `CodexEventReducer`。
- 有 `pendingApproval` 时调用 `showApproval`。

- [ ] **步骤 3：重新验证当前 app-server response 协议**

这一步不能猜。必须先运行 spike：

```bash
CODEX_APP_SERVER_PORT=8794 node prototypes/app-server-approval/app-server-approval-spike.mjs
```

记录当前 Codex app-server 对 approval response 的方法名、request id 和 payload 结构。

- [ ] **步骤 4：实现 decision 回传**

要求：

- 使用步骤 3 验证到的真实 response shape。
- 回传 `ApprovalAction.decision` 的原始 payload。
- 不在 app 层重新解释 decision。

- [ ] **步骤 5：端到端验证**

流程：

1. 启动 Codex app-server。
2. 启动 Codex Status Radar。
3. 触发一个需要审批的 Codex 命令。
4. 灵动岛弹窗出现。
5. 从灵动岛点击批准或拒绝。
6. Codex 原流程继续或取消。

- [ ] **步骤 6：提交**

```bash
git add apps/macos/Sources/CodexStatusRadarApp README.md docs/research/app-server-approval-spike.md
git commit -m "Connect live approval flow"
git push
```

## 任务 9：设置、隐私和遥测开关

**目标：** 建立设置存储，明确遥测默认关闭，保证隐私边界可配置、可说明。

**文件：**

- 创建：`apps/macos/Sources/CodexStatusRadarApp/SettingsStore.swift`
- 创建：`apps/macos/Tests/CodexStatusRadarAppTests/SettingsStoreTests.swift`
- 更新：`README.md`
- 更新：`docs/architecture/technical-architecture.md`

- [ ] **步骤 1：写失败测试**

测试内容：

- `telemetryEnabled` 默认是 `false`。
- endpoint 默认是 `ws://127.0.0.1:8794`。
- 重置后恢复默认值。

- [ ] **步骤 2：实现 `SettingsStore`**

要求：

- 使用 `UserDefaults`。
- 遥测默认关闭。
- 不保存 token、secret、命令、路径或文件名。

- [ ] **步骤 3：更新 README 隐私说明**

必须明确：

- 本产品本地优先。
- 默认不上传任何用户数据。
- 遥测默认关闭。
- 未来即使开启遥测，也不能上传源码、prompt、命令、diff、文件名、完整路径、token、secret 或原始 app-server payload。

- [ ] **步骤 4：验证并提交**

```bash
swift test --disable-sandbox
node --check prototypes/app-server-approval/app-server-approval-spike.mjs
git diff --check
git add apps/macos/Sources/CodexStatusRadarApp/SettingsStore.swift \
  apps/macos/Tests/CodexStatusRadarAppTests/SettingsStoreTests.swift \
  README.md docs/architecture/technical-architecture.md
git commit -m "Add privacy-first settings gate"
git push
```

## 任务 10：发布前整理

**目标：** 在公开发布或分发二进制前，确认代码、文档、隐私和许可证都达标。

- [ ] **步骤 1：全量验证**

```bash
swift test --disable-sandbox
swift build --disable-sandbox
node --check prototypes/app-server-approval/app-server-approval-spike.mjs
git diff --check
git status --porcelain=v1
```

预期：

- 测试通过。
- 构建通过。
- prototype 语法检查通过。
- diff 无格式问题。
- 提交后工作区干净。

- [ ] **步骤 2：许可证检查**

如果复制或 fork 了第三方项目的代码、UI 文案、结构、资产：

- 必须保留对应许可证。
- 必须保留必要版权声明。
- 必须更新 `docs/decisions/0002-license-checkpoints.md`。

如果仍是 clean-room 实现：

- 发布前主动选择项目许可证。
- 在 README 中明确说明。

- [ ] **步骤 3：推送**

```bash
git push
```

## 执行规则

- 每个任务独立提交。
- 每个功能先有失败测试。
- 不跳过验证。
- 不为了赶进度把 app 层逻辑塞进 core。
- 不把 prototype 代码直接搬进生产代码。
- 不上传敏感信息。
- 默认中文写文档、计划和协作说明。
